apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rhtas-operator
  namespace: openshift-operators
spec:
  channel: {{ rhtas_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: rhtas-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  startingCSV: {{ rhtas_operator_subscription_starting_csv | default('""', true) }}
