apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rhtpa-operator
  namespace: trusted-profile-analyzer
spec:
  channel: {{ rhtpa_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: rhtpa-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  startingCSV: {{ rhtpa_operator_subscription_starting_csv | default('""', true) }}
